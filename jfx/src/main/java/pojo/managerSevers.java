package pojo;

import java.util.ArrayList;

/*public class managerSevers {
    public managerSevers() {
    }
    private static ArrayList<manager> mgs=new ArrayList<>();
    static {
        mgs.add(new manager("0001","2002"));
       mgs.add(new manager("0002","2002"));
        mgs.add(new manager("0003","2002"));
    }
    public manager findByusername(String username) {
        for (manager m:mgs) {
            if(username.equals(m.getName())){
                return m;
            }
        }
        return null;
    }
}

 */
